---
layout: /src/layouts/Layout.astro
title: "麻烦的 API 文档化，给代码就能搞定 Swagger"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "编码/开发"
description: "粘贴 API 代码就能自动转换为 OpenAPI (Swagger) 规范或 Markdown 文档的提示词。"
tags: ["API", "文档化", "Swagger", "OpenAPI", "后端"]
---

# 📝 麻烦的 API 文档化，给代码就能搞定 Swagger

**🎯 推荐对象:** 所有人
**⏱️ 所需时间:** 5 分钟
**🤖 推荐模型:** 所有 AI 模型

|  难度   |   有效性   |  实用性   |
| :-----: | :--------: | :-------: |
| ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |

_“开发完了得给文档吧？（前端开发者）”_
_“啊... 请稍等。（还没写一行）”_

比 API 开发更讨厌写文档的，只有我吗？
参数类型、是否必须、响应示例，一个个写下来真的会崩溃。
现在只要把控制器代码复制粘贴过来。AI 会为你转换为整洁的 Swagger 规范。

---

## ⚡️ 3 行总结 (TL;DR)

1. 基于代码提取准确的参数/响应明细
2. 自动生成 OpenAPI (Swagger) YAML/JSON 格式
3. 包含易于理解的说明和示例数据

---

## 🚀 解决方案：“API 文档生成器”

### 🥉 Basic Version (基础版)

当你只需要快速结果时使用。

> **角色:** 你是细致的技术作家兼后端开发者。
> **请求:** 请制作粘贴 API 代码就能自动转换为 OpenAPI (Swagger) 规范或 Markdown 文档的提示词。

<br>

### 🥇 Pro Version (专家版)

当你需要详细质量时使用。

请复制以下 **PROMPT** 内容使用。

> **角色 (Role):** 你是细致的技术作家兼后端开发者。
>
> **情况 (Context):** 我有开发好的 API 代码，为了与前端开发者共享，需要将其文档化。
>
> **任务 (Task):**
> 分析下面提供的 API 代码，以 **[选择想要的格式: OpenAPI 3.0 YAML / Markdown Table]** 形式编写文档。
> 必须包含以下内容:
>
> 1. **Endpoint & Method:** URL 路径和 HTTP 方法
> 2. **Summary:** 关于 API 作用的简略总结
> 3. **Request Parameters:** Body, Query, Path 参数（包含类型、是否必须、说明）
> 4. **Response:** 成功 (200) 及失败 (400, 500 等) 时的响应结构和示例 JSON
>
> **代码:**
>
> ```
> [请在此处粘贴控制器/路由器代码]
> ```
>
> **约束事项 (Constraints):**
>
> - 请明确标明数据类型 (String, Integer 等)。
> - 响应示例 (Example Value) 请用现实的数据填充。

---

## 🧬 提示词解剖 (Why it works?)

---

## 📊 证明: Before & After

### ❌ Before (输入代码 - Node.js Express)

```javascript
app.post("/users", (req, res) => {
  const { username, email } = req.body;
  if (!email) return res.status(400).send({ msg: "no email" });
  // DB 保存逻辑...
  res.status(201).send({ id: 1, username, email });
});
```

### ✅ After (结果 - Markdown 示例)

### **POST /users**

用户注册 API

**Request Body**
| 字段名 | 类型 | 必须 | 说明 |
|---|---|---|---|
| `username` | String | O | 用户昵称 |
| `email` | String | O | 用户邮箱 (唯一) |

**Response (201 Created)**

```json
{
  "id": 1,
  "username": "jay_dev",
  "email": "jay@example.com"
}
```

---

## 🎯 结论

文档化不是开发的结束，而是协作的开始。
但不要为了那个开始消耗太多精力。交给 AI，大家专注于业务逻辑吧！🍷
