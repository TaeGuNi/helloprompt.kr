---
layout: /src/layouts/Layout.astro
title: "Медленный SQL-запрос? Тюнинг за 3 секунды"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Разработка"
description: "Промпт, который анализирует сложные и неэффективные SQL-запросы, оптимизирует производительность и рекомендует индексы."
tags: ["SQL", "ТюнингБД", "ОптимизацияЗапросов", "БазаДанных"]
lang: ru
---

# 📝 Медленный SQL-запрос? Тюнинг за 3 секунды

> **🎯 Рекомендуется для:** Всех
> **⏱️ Время:** 5 минут
> **🤖 Рекомендуемая модель:** Все модели ИИ

| Сложность | Эффективность | Полезность |
| :-------: | :-----------: | :--------: |
|  ⭐⭐☆☆☆  |  ⭐⭐⭐⭐⭐   | ⭐⭐⭐⭐☆  |

_"Данных стало чуть больше, и запрос выполняется 5 секунд..."_

Сначала запрос был быстрым, но по мере роста данных он превращается в черепаху.
Смотрели на план выполнения (Explain Plan), но видели только черные буквы на белом фоне и чувствовали растерянность?
Покажите запрос ИИ, он найдет неэффективные части и даже порекомендует волшебные индексы.

---

## ⚡️ Краткое содержание (TL;DR)

1. Улучшение неэффективных Join и подзапросов.
2. Рекомендация необходимых индексов (Index).
3. Переписывание на читаемый современный синтаксис SQL.

---

## 🚀 Решение: "Тюнер SQL-запросов"

### 🥉 Базовая версия (Basic Version)

Используйте, когда вам нужен быстрый результат.

> **Роль:** Ты старший администратор баз данных (DBA) с 20-летним стажем и эксперт по тюнингу SQL.
> **Запрос:** Проанализируй сложный и неэффективный SQL-запрос, оптимизируй производительность и порекомендуй индексы.

<br>

### 🥇 Про-версия (Pro Version)

Используйте, когда вам нужно детальное качество.

Скопируйте **PROMPT** ниже и вставьте его в ChatGPT или Claude.

> **Роль:** Ты старший администратор баз данных (DBA) с 20-летним стажем и эксперт по тюнингу SQL.
>
> **Ситуация:** SQL-запрос, который я использую в своем приложении, вызывает снижение производительности при увеличении объема данных.
>
> **Запрос:**
> Проанализируй предоставленный ниже SQL-запрос и выполни следующие задачи:
>
> 1. **Диагностика проблем:** Укажи части текущего запроса, которые могут вызывать снижение производительности (например, Full Table Scan).
> 2. **Оптимизация запроса:** Перепиши запрос так, чтобы он возвращал тот же результат, но работал эффективнее.
> 3. **Рекомендация индексов:** Предложи с помощью оператора `CREATE INDEX`, на какие столбцы следует наложить индексы для максимальной производительности запроса.
>
> **Используемая БД:** `[MySQL / PostgreSQL / Oracle / MS-SQL]`
>
> **Целевой запрос:**
>
> ```sql
> [Вставьте сюда медленный запрос]
> ```
>
> **Ограничения:**
>
> - Результирующий набор данных не должен изменяться.
> - Кратко объясни принцип работы и почему стало быстрее.

---

## 🧬 Разбор промпта (Почему это работает?)

---

## 📊 Доказательство: До и После

### ❌ До (Ввод запроса)

```sql
SELECT * FROM users
WHERE id IN (SELECT user_id FROM orders WHERE price > 10000);
```

_Проблема: Подзапрос может выполняться для каждой строки (Dependent Subquery)._

### ✅ После (Результат)

**Оптимизированный запрос (использование JOIN):**

```sql
SELECT DISTINCT u.*
FROM users u
INNER JOIN orders o ON u.id = o.user_id
WHERE o.price > 10000;
```

**Рекомендуемый индекс:**

```sql
CREATE INDEX idx_orders_price_userid ON orders(price, user_id);
```

_Объяснение: Оптимизировали, заменив подзапрос на JOIN, и создали составной индекс для фильтрации по `price` и последующего соединения по `user_id`._

---

## 🎯 Заключение

Говорят, "тюнинг — это искусство", но теперь это "наука", в которой можно заручиться поддержкой ИИ.
Не страдайте из-за медленных запросов, получите бесплатную консультацию у ИИ-DBA! 🍷
