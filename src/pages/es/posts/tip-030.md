---
layout: /src/layouts/Layout.astro
title: "¡JSON a TypeScript al instante! (feat. Zod)"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Desarrollo"
description: "Un prompt que convierte automáticamente datos JSON complejos en interfaces TypeScript o esquemas Zod."
tags: ["TypeScript", "Zod", "JSON", "Definición de tipos", "Productividad"]
lang: es
---

# 📝 ¡JSON a TypeScript al instante! (feat. Zod)

> **🎯 Recomendado para:** Todos
> **⏱️ Tiempo:** 5 minutos
> **🤖 Modelo recomendado:** Todos los modelos de IA

| Dificultad | Efectividad | Utilidad  |
| :--------: | :---------: | :-------: |
|  ⭐⭐☆☆☆   | ⭐⭐⭐⭐⭐  | ⭐⭐⭐⭐☆ |

_"La respuesta de la API del backend es muy compleja, ¿cuándo voy a crear todas las interfaces?"_

¿Estás escribiendo `interface User { ... }` uno por uno?
Objetos anidados, arrays, valores opcionales... es fácil cometer errores.
Solo copia y pega el JSON de respuesta de la API. Te crearemos definiciones de tipos TypeScript perfectas e incluso esquemas Zod para validación en tiempo de ejecución.

---

## ⚡️ Resumen en 3 líneas (TL;DR)

1. Conversión de datos JSON a Interface/Type de TypeScript.
2. Generación de esquemas Zod/Yup para validación en tiempo de ejecución.
3. Manejo perfecto de estructuras anidadas (Nested Object).

---

## 🚀 Solución: "Maestro de Tipos"

### 🥉 Versión Básica

Úsala cuando necesites resultados rápidos.

> **Rol:** Eres un desarrollador principal experto en el ecosistema TypeScript.
> **Solicitud:** Un prompt que convierte automáticamente datos JSON complejos en interfaces TypeScript o esquemas Zod.

<br>

### 🥇 Versión Pro (Experto)

Úsala cuando necesites calidad detallada.

Copia y pega el siguiente **PROMPT** en ChatGPT o Claude.

> **Rol:** Eres un desarrollador principal experto en el ecosistema TypeScript.
>
> **Situación:** He recibido datos JSON de una API externa y necesito definiciones de tipos para usarlos de forma segura en mi proyecto.
>
> **Solicitud:**
>
> 1. Escribe una **Interface de TypeScript** basada en los datos JSON a continuación. (Usa PascalCase para los nombres)
> 2. Escribe también un **Esquema Zod (Schema)** para validación en tiempo de ejecución.
> 3. Incluye código para inferir el tipo desde el esquema Zod (`z.infer`).
>
> **Datos JSON:**
>
> ```text
> [Pega aquí los datos JSON]
> ```
>
> **Restricciones:**
>
> - Asume que todos los campos son obligatorios por defecto, pero trátalos como opcionales (`?`) solo si contienen `null`.
> - Separa los objetos dentro de arrays en interfaces independientes.
> - Añade comentarios JSDoc explicando qué es cada campo (infiriéndolo).

---

## 🧬 Anatomía del Prompt (Why it works?)

---

## 📊 Prueba: Antes y Después

### ❌ Antes (JSON de entrada)

```text
{
  "id": 1,
  "name": "Jay",
  "contact": {
    "email": "jay@test.com",
    "phone": null
  },
  "tags": ["developer", "admin"]
}
```

### ✅ Después (Resultado)

```typescript
import { z } from "zod";

// 1. Zod Schema Definition
export const ContactSchema = z.object({
  email: z.string().email(),
  phone: z.string().nullable().optional(), // tratamiento de null
});

export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  contact: ContactSchema,
  tags: z.array(z.string()),
});

// 2. TypeScript Type Inference
/** Interfaz de información de usuario */
export type User = z.infer<typeof UserSchema>;
export type Contact = z.infer<typeof ContactSchema>;
```

---

## 🎯 Conclusión

La definición de tipos es un seguro, pero el proceso de crearla no debería ser doloroso.
¡Construye un sistema de tipos seguro y robusto con un solo copiar y pegar! 🍷
