---
layout: /src/layouts/Layout.astro
title: "Просто бросьте JSON, и определение типа TypeScript появится! (feat. Zod)"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Кодинг/Разработка"
description: "Промпт, который автоматически преобразует сложные данные JSON в интерфейсы TypeScript или схемы Zod."
tags: ["TypeScript", "Zod", "JSON", "Определение типа", "Продуктивность"]
---

# 📝 Просто бросьте JSON, и определение типа TypeScript появится! (feat. Zod)

**🎯 Рекомендуется для:** Всех
**⏱️ Требуемое время:** 5 минут
**🤖 Рекомендуемая модель:** Все модели ИИ

| Сложность | Эффективность | Полезность |
| :-------: | :-----------: | :--------: |
|  ⭐⭐☆☆☆  |  ⭐⭐⭐⭐⭐   | ⭐⭐⭐⭐☆  |

_"Ответ API бэкенда такой сложный, когда я сделаю все эти интерфейсы?"_

Вы печатаете `interface User { ... }` по одному?
Вложенные объекты, массивы, необязательные значения... идеально для ошибок.
Просто скопируйте и вставьте JSON ответа API. Он создает идеальные определения типов TypeScript, а также схемы Zod для валидации во время выполнения.

---

## ⚡️ Краткое содержание в 3 строках (TL;DR)

1. Преобразование данных JSON в интерфейс/тип TypeScript
2. Генерация схемы Zod/Yup для валидации во время выполнения
3. Идеальная обработка вложенных структур (Вложенный объект)

---

## 🚀 Решение: "Мастер типов"

### 🥉 Базовая версия

Используйте это, когда вам нужен просто быстрый результат.

**Роль:** Ты старший разработчик, освоивший экосистему TypeScript.
**Запрос:** Создай промпт, который автоматически преобразует сложные данные JSON в интерфейсы TypeScript или схемы Zod.

<br>

### 🥇 Профессиональная версия (Эксперт)

Используйте это, когда вам нужно детальное качество.

Скопируйте содержание **PROMPT** ниже и вставьте его в ChatGPT или Claude.

**Роль:** Ты старший разработчик, освоивший экосистему TypeScript.

**Контекст:** Я получил данные JSON от внешнего API, и мне нужны определения типов, чтобы безопасно использовать их в моем проекте.

**Задача:**

1. Напиши **Интерфейс TypeScript** на основе данных JSON ниже. (Имя в PascalCase)
2. Напиши **Схему Zod** вместе для валидации во время выполнения.
3. Включи код для вывода типов (`z.infer`) из схемы Zod.

**Данные JSON:**

```text
[Вставьте данные JSON сюда]
```

**Ограничения:**

- Предполагай, что все поля обязательны по умолчанию, но рассматривай их как необязательные (`?`) только в том случае, если включен `null`.
- Раздели объекты внутри массивов на отдельные интерфейсы и определи их.
- Добавь комментарии JSDoc, чтобы объяснить, что представляет собой каждое поле (путем вывода).

---

## 🧬 Анатомия промпта (Почему это работает?)

---

## 📊 Доказательство: До и После

### ❌ До (Входной JSON)

```text
{
  "id": 1,
  "name": "Jay",
  "contact": {
    "email": "jay@test.com",
    "phone": null
  },
  "tags": ["developer", "admin"]
}
```

### ✅ После (Результат)

```typescript
import { z } from "zod";

// 1. Zod Schema Definition
export const ContactSchema = z.object({
  email: z.string().email(),
  phone: z.string().nullable().optional(), // Null handling
});

export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  contact: ContactSchema,
  tags: z.array(z.string()),
});

// 2. TypeScript Type Inference
/** User Information Interface */
export type User = z.infer<typeof UserSchema>;
export type Contact = z.infer<typeof ContactSchema>;
```

---

## 🎯 Заключение

Определение типа — это устройство безопасности, но процесс его создания не должен быть болезненным.
Постройте безопасную и надежную систему типов одним копированием и вставкой! 🍷
