---
title: "JSONだけ投げればTypeScript型定義がパッ！(feat. Zod)"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "コーディング/開発"
description: "複雑なJSONデータをTypeScriptインターフェースやZodスキーマに自動変換してくれるプロンプトです。"
tags: ["TypeScript", "Zod", "JSON", "型定義", "生産性"]
---

# 📝 JSONだけ投げればTypeScript型定義がパッ！(feat. Zod)

**🎯 推奨対象:** 誰でも
**⏱️ 所要時間:** 5分
**🤖 推奨モデル:** すべてのAIモデル

| 難易度  |    効果    |  活用度   |
| :-----: | :--------: | :-------: |
| ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |

_「バックエンドAPIレスポンスがこんなに複雑なのに、これをいつ全部インターフェースにするんだ？」_

`interface User { ... }` いちいち打っていますか？
ネストされたオブジェクト、配列、オプショナル値... ミスするのにぴったりです。
APIレスポンスJSONだけコピーして入れてください。完璧なTypeScript型定義はもちろん、ランタイム検証のためのZodスキーマまで作って差し上げます。

---

## ⚡️ 3行要約 (TL;DR)

1. JSONデータをTypeScript Interface/Typeに変換
2. ランタイム有効性検査のためのZod/Yupスキーマ生成
3. ネストされた構造(Nested Object)も完璧処理

---

## 🚀 解決策：「型マスター」

### 🥉 Basic Version (基本形)

素早く結果だけが必要な場合に使用してください。

> **役割:** あなたはTypeScriptエコシステムに精通した首席開発者です。
> **リクエスト:** 複雑なJSONデータをTypeScriptインターフェースやZodスキーマに自動変換してくれるプロンプトを作成してください。

<br>

### 🥇 Pro Version (専門家形)

詳細なクオリティが必要な場合に使用してください。

以下の**PROMPT**内容をコピーして使用してください。

> **役割:** あなたはTypeScriptエコシステムに精通した首席開発者です。
>
> **状況:** 外部APIからJSONデータを受け取ったが、これを私のプロジェクトで安全に使用するために型定義が必要だ。
>
> **タスク:**
>
> 1. 以下のJSONデータに基づいて**TypeScript Interface**を作成してください。(名前はパスカルケースで)
> 2. ランタイム検証のために**Zodスキーマ(Schema)**も一緒に作成してください。
> 3. Zodスキーマから型を推論(`z.infer`)するコードも含めてください。
>
> **JSONデータ:**

> [ここにJSONデータを貼り付けてください]

> **制約事項:**
>
> - 全てのフィールドは基本的に必須(required)と仮定するが、`null`が含まれる場合のみオプショナル(`?`)で処理して。
> - 配列の中にあるオブジェクトも別途のインターフェースに分離して定義して。
> - JSDocコメントをつけて各フィールドが何なのか(推論して)説明して。

---

## 🧬 プロンプト解剖 (Why it works?)

---

## 📊 証明: Before & After

### ❌ Before (入力JSON)

```text
{
  "id": 1,
  "name": "Jay",
  "contact": {
    "email": "jay@test.com",
    "phone": null
  },
  "tags": ["developer", "admin"]
}
```

### ✅ After (結果)

```typescript
import { z } from "zod";

// 1. Zod Schema Definition
export const ContactSchema = z.object({
  email: z.string().email(),
  phone: z.string().nullable().optional(), // null 処理
});

export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  contact: ContactSchema,
  tags: z.array(z.string()),
});

// 2. TypeScript Type Inference
/** ユーザー情報インターフェース */
export type User = z.infer<typeof UserSchema>;
export type Contact = z.infer<typeof ContactSchema>;
```

---

## 🎯 結論

型定義は安全装置ですが、それを作る過程が苦痛であってはなりません。
コピペ一回で安全で丈夫な型システムを構築してください！ 🍷
