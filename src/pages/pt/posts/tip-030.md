---
layout: /src/layouts/Layout.astro
title: "JSON Vira Definição de Tipo TypeScript num Piscar de Olhos! (feat. Zod)"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Desenvolvimento"
description: "Um prompt que converte automaticamente dados JSON complexos em interfaces TypeScript ou esquemas Zod."
tags: ["TypeScript", "Zod", "JSON", "Definição de Tipo", "Produtividade"]
lang: pt
---

# 📝 JSON Vira Definição de Tipo TypeScript num Piscar de Olhos! (feat. Zod)

> **🎯 Recomendado para:** Todos
> **⏱️ Tempo necessário:** 5 minutos
> **🤖 Modelo recomendado:** Todos os modelos de IA

| Dificuldade |  Eficácia  | Utilidade |
| :---------: | :--------: | :-------: |
|   ⭐⭐☆☆☆   | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |

_"A resposta da API backend é tão complexa, quando vou criar interface para tudo isso?"_

Você está digitando `interface User { ... }` um por um?
Objetos aninhados, arrays, valores opcionais... é fácil cometer erros.
Apenas copie e cole o JSON da resposta da API. A IA cria definições de tipo TypeScript perfeitas e até esquemas Zod para validação em tempo de execução.

---

## ⚡️ 3줄 요약 (TL;DR)

1. Conversão de dados JSON para Interface/Type TypeScript
2. Geração de esquema Zod/Yup para validação em tempo de execução
3. Tratamento perfeito até de estruturas aninhadas (Nested Object)

---

## 🚀 Solução: "Mestre dos Tipos"

### 🥉 Versão Básica

Use quando precisar apenas de resultados rápidos.

> **Papel:** Você é um Desenvolvedor Chefe especialista no ecossistema TypeScript.
> **Tarefa:** Um prompt que converte automaticamente dados JSON complexos em interfaces TypeScript ou esquemas Zod.

<br>

### 🥇 Versão Pro (Especialista)

Use quando precisar de qualidade detalhada.

Abaixo copie o conteúdo do **PROMPT** e use.

> **Papel:** Você é um Desenvolvedor Chefe especialista no ecossistema TypeScript.
>
> **Situação:** Recebi dados JSON de uma API externa e preciso de definições de tipo para usá-los com segurança no meu projeto.
>
> **Tarefa:**
>
> 1. Com base nos dados JSON abaixo, escreva uma **Interface TypeScript**. (Use PascalCase para o nome)
> 2. Escreva também um **Esquema Zod (Schema)** para validação em tempo de execução.
> 3. Inclua o código que infere o tipo a partir do esquema Zod (`z.infer`).
>
> **Dados JSON:**
>
> ```
> [Cole aqui os dados JSON]
> ```
>
> **Restrições:**
>
> - Assuma que todos os campos são obrigatórios (required) por padrão, mas trate como opcional (`?`) apenas se contiver `null`.
> - Separe objetos dentro de arrays em interfaces separadas.
> - Adicione comentários JSDoc explicando o que é cada campo (inferindo).

---

## 🧬 Análise do Prompt (Por que funciona?)

---

## 📊 Prova: Antes e Depois

### ❌ Antes (Entrada JSON)

```json
{
  "id": 1,
  "name": "Jay",
  "contact": {
    "email": "jay@test.com",
    "phone": null
  },
  "tags": ["developer", "admin"]
}
```

### ✅ Depois (Resultado)

```typescript
import { z } from "zod";

// 1. Zod Schema Definition
export const ContactSchema = z.object({
  email: z.string().email(),
  phone: z.string().nullable().optional(), // tratamento de null
});

export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  contact: ContactSchema,
  tags: z.array(z.string()),
});

// 2. TypeScript Type Inference
/** Interface de informações do usuário */
export type User = z.infer<typeof UserSchema>;
export type Contact = z.infer<typeof ContactSchema>;
```

---

## 🎯 Conclusão

Definição de tipos é um dispositivo de segurança, mas o processo de criá-la não deve ser doloroso.
Construa um sistema de tipos seguro e robusto com um único copiar e colar! 🍷
