---
layout: /src/layouts/Layout.astro
title: "麻烦的API文档化，只要给代码Swagger立马搞定"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "编程/开发"
description: "只要粘贴API代码，就能自动转换为OpenAPI(Swagger)规范或Markdown文档的提示词。"
tags: ["API", "文档化", "Swagger", "OpenAPI", "后端"]
---

# 📝 麻烦的API文档化，只要给代码Swagger立马搞定

> **🎯 推荐对象:** 所有人
> **⏱️ 所需时间:** 5分钟
> **🤖 推荐模型:** 所有AI模型

|  难度   |   有效性   |  实用性   |
| :-----: | :--------: | :-------: |
| ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |

_"开发完了得给文档吧？ (前端开发者)"_
_"啊... 稍等。(还一行没写)"_

比起开发API更讨厌写文档，只有我这样吗？
参数类型、是否必须、响应示例一个个写，真是怀疑人生。
现在只要把Controller代码刮下来粘贴进去。AI会为你转换为整洁的Swagger规范。

---

## ⚡️ 3行摘要 (TL;DR)

1. 基于代码提取准确的参数/响应规范
2. 自动生成OpenAPI(Swagger) YAML/JSON 格式
3. 包含易懂的说明和示例数据

---

## 🚀 解决方案: "API文档生成器"

### 🥉 Basic Version (基础版)

当只需要快速获得结果时使用。

> **角色:** 你是细致的技术文档撰写人兼后端开发者。
> **请求:** 只要粘贴API代码，就能自动转换为OpenAPI(Swagger)规范或Markdown文档的提示词。

<br>

### 🥇 Pro Version (专家版)

当需要细节质量时使用。

请复制下方 **PROMPT** 内容并使用。

> **角色 (Role):** 你是细致的技术文档撰写人兼后端开发者。
>
> **情况 (Context):** 我有开发好的API代码，为了和前端开发者共享，需要文档化。
>
> **请求 (Task):**
> 分析下面提供的API代码，以 **[选择想要的格式: OpenAPI 3.0 YAML / Markdown Table]** 形式编写文档。
> 必须包含以下内容:
>
> 1. **Endpoint & Method:** URL 路径和 HTTP 方法
> 2. **Summary:** 关于API做的事情的简略摘要
> 3. **Request Parameters:** Body, Query, Path 参数 (包含类型, 是否必须, 说明)
> 4. **Response:** 成功(200)及失败(400, 500等)时的响应结构和示例JSON
>
> **代码:**
>
> ```
> [在此粘贴Controller/Router代码]
> ```
>
> **约束事项 (Constraints):**
>
> - 请明确标明数据类型(String, Integer等)。
> - 响应示例(Example Value)请用现实的数据填充。

---

## 🧬 提示词解剖 (Why it works?)

---

## 📊 证明: Before & After

### ❌ Before (输入代码 - Node.js Express)

```javascript
app.post("/users", (req, res) => {
  const { username, email } = req.body;
  if (!email) return res.status(400).send({ msg: "no email" });
  // DB 저장 로직...
  res.status(201).send({ id: 1, username, email });
});
```

### ✅ After (结果 - Markdown 示例)

### **POST /users**

用户注册 API

**Request Body**
| 字段名 | 类型 | 必须 | 说明 |
|---|---|---|---|
| `username` | String | O | 用户昵称 |
| `email` | String | O | 用户邮箱 (唯一) |

**Response (201 Created)**

```json
{
  "id": 1,
  "username": "jay_dev",
  "email": "jay@example.com"
}
```

---

## 🎯 结论

文档化不是开发的结束，而是协作的开始。
但不要为了那个开始消耗太多精力。交给AI，你们专注于业务逻辑吧！ 🍷
