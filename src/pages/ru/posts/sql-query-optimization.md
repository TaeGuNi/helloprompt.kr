---
layout: /src/layouts/Layout.astro
title: "Оптимизация SQL-запросов, пока БД не закричала"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Разработка"
description: "Промпт, который анализирует медленные и сложные SQL-запросы для оптимизации производительности и предложения подсказок по индексам."
tags: ["SQL", "Тюнинг БД", "Бэкенд"]
---

# 📝 Оптимизация SQL-запросов, пока БД не закричала

**🎯 Рекомендуется для:** Всех
**⏱️ Требуемое время:** 5 минут
**🤖 Рекомендуемая модель:** Все модели ИИ

| Сложность | Эффективность | Полезность |
| :-------: | :-----------: | :--------: |
|  ⭐⭐☆☆☆  |  ⭐⭐⭐⭐⭐   | ⭐⭐⭐⭐☆  |

_"Загрузка списка доски занимает 3 секунды? Если запрос запутан, и вы не знаете, с чего начать."_

Нелегко найти причину снижения производительности среди пира сложных Join и подзапросов. Позвольте ИИ предсказать план выполнения запроса и получить предложения по лучшему написанию.

---

## ⚡️ Краткое содержание в 3 строках (TL;DR)

1. Нелегко найти причину снижения производительности из-за сложных SQL-запросов.
2. Получите диагностику узких мест, заставив ИИ предсказать план выполнения запроса.
3. Вы можете значительно улучшить производительность БД с помощью оптимизированных запросов и рекомендаций по индексам.

---

## 🚀 Решение: "Тюнер запросов DBA"

### 🥉 Базовая версия

Используйте это, когда вам нужен просто быстрый результат.

**Роль:** Ты `[15-летний DBA (Администратор базы данных)]`.
**Запрос:** Создай промпт, который анализирует медленные и сложные SQL-запросы для оптимизации производительности и предложения подсказок по индексам.

<br>

### 🥇 Профессиональная версия (Эксперт)

Используйте это, когда вам нужно детальное качество.

Скопируйте содержание **PROMPT** ниже и вставьте его в ChatGPT или Claude.

**Роль:** Ты `[15-летний DBA (Администратор базы данных)]`.

**Ситуация:** SQL-запрос, который я написал, становится слишком медленным по мере увеличения объема данных.

**Задача:**

1. Укажи на потенциальные узкие места производительности в этом запросе (неэффективные join, неиспользуемые индексы и т.д.).
2. Напиши оптимизированный запрос с улучшенной производительностью.
3. Порекомендуй, на каких столбцах создать индекс для быстрого выполнения этого запроса.

**Используемая БД:** `[MySQL / PostgreSQL / Oracle и т.д.]`
**Текущий запрос:**
`[Вставьте ваш медленный SQL-запрос сюда]`

---

## 🧬 Анатомия промпта (Почему это работает?)

---

## 📊 Доказательство: До и После

### ❌ До (Ввод)

```sql
SELECT * FROM Users u
WHERE u.id IN (SELECT user_id FROM Orders WHERE created_at > '2024-01-01')
-- Предполагается неэффективная ситуация с использованием подзапроса
```

<br>

### ✅ После (Результат)

```sql
-- Оптимизированный запрос (Использование INNER JOIN)
SELECT DISTINCT u.*
FROM Users u
INNER JOIN Orders o ON u.id = o.user_id
WHERE o.created_at > '2024-01-01';

/*
💡 Пункты улучшения:
1. Удален подзапрос в предложении IN: Подзапросы в предложениях IN являются главным виновником снижения производительности при запросе больших объемов данных.
   Заменено на JOIN, чтобы облегчить оптимизацию движком БД.
2. Избегайте SELECT *: На практике лучше указывать только необходимые столбцы (например, u.name, u.email).

🚀 Рекомендации по индексам:
- Столбец `created_at` таблицы Orders (Эффективный поиск по диапазону)
- Столбец `user_id` таблицы Orders (Улучшение производительности Join)
*/
```

---

## 🎯 Заключение

Тюнинг запросов — это область опыта, но ИИ изучил бесчисленные лучшие практики. Оставьте отправную точку тюнинга ИИ.
