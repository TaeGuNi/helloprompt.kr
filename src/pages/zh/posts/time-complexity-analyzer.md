---
layout: /src/layouts/Layout.astro
title: "我的代码的时间复杂度 (Big O) 是多少？"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "编码/开发"
description: "分析编写的算法的时间复杂度和空间复杂度，并找出性能改进点的提示词。"
tags: ["算法", "时间复杂度", "Big-O", "性能优化", "CS知识"]
---

# 📝 我的代码的时间复杂度 (Big O) 是多少？

> **🎯 推荐对象:** 所有人
> **⏱️ 所需时间:** 5 分钟
> **🤖 推荐模型:** 所有 AI 模型

|  难度   |   有效性   |  实用性   |
| :-----: | :--------: | :-------: |
| ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |

_“两个 for 循环重叠... 这是 O(N) 还是 O(N^2)？”_

参加编程测试或编写处理大容量流量的逻辑时，时间复杂度 (Time Complexity) 分析是必须的。
但是循环里有递归调用或者条件变得复杂的话，计算起来很头疼吧？
给 AI 看代码，它会用 Big-O 表示法准确分析，甚至建议更高效的算法。

---

## ⚡️ 3 行总结 (TL;DR)

1. 准确计算时间复杂度及空间复杂度 (Big-O)
2. 识别性能瓶颈区间 (Bottleneck)
3. 建议具有更低复杂度的改进算法

---

## 🚀 解决方案：“复杂度分析器”

### 🥉 Basic Version (基础版)

当你只需要快速结果时使用。

> **角色:** 你是计算机科学 (CS) 教授兼算法优化专家。
> **请求:** 请制作分析编写的算法的时间复杂度和空间复杂度，并找出性能改进点的提示词。

<br>

### 🥇 Pro Version (专家版)

当你需要详细质量时使用。

请复制以下 **PROMPT** 内容使用。

> **角色 (Role):** 你是计算机科学 (CS) 教授兼算法优化专家。
>
> **情况 (Context):** 我很好奇我编写的算法的性能。想知道它是高效的，还是需要改进。
>
> **任务 (Task):**

- 1. 用 Big-O 表示法分析提供的代码的**时间复杂度 (Time Complexity)**和**空间复杂度 (Space Complexity)**。
     > 2. 请按代码行说明为什么会出现这种复杂度的依据。
     > 3. 如果有更高效的方法 (例: O(N^2) -> O(N log N))，请连同改进的代码一起说明。
     >
     > **代码:**
     >
     > ```
     > [请在此处粘贴要分析的代码]
     > ```
     >
     > **约束事项 (Constraints):**
     >
     > - 请以最坏情况 (Worst Case) 为基准进行分析。
     > - 请提及输入数据的大小 (N) 非常大时可能发生的问题。

---

## 🧬 提示词解剖 (Why it works?)

---

## 📊 证明: Before & After

### ❌ Before (输入代码)

```python
def find_duplicate(nums):
    for i in range(len(nums)):
        for j in range(i + 1, len(nums)):
            if nums[i] == nums[j]:
                return nums[i]
    return None
```

### ✅ After (结果)

**分析:**

- **时间复杂度:** O(N^2)。正在使用双重 `for` 循环。即使 N 只有 10,000 也会变得非常慢。
- **空间复杂度:** O(1)。几乎不使用额外的内存。

**改进的代码 (使用 Hash Set):**

```python
def find_duplicate_optimized(nums):
    seen = set()
    for num in nums:
        if num in seen:
            return num
        seen.add(num)
    return None
```

**改进结果:**

- **时间复杂度:** O(N)。循环只转一次。
- **空间复杂度:** O(N)。必须将数据存储在 `seen` 集合中。(Time-Space Tradeoff)

---

## 🎯 结论

“能跑就行”的想法现在停止吧。
连看不见的性能也照顾到的开发者才是真正的高手。请用 AI 教授的反馈培养算法肌肉！🍷
