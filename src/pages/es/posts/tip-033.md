---
layout: /src/layouts/Layout.astro
title: "Construcción de entorno Docker, listo en 3 minutos con un archivo de configuración"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Desarrollo"
description: "Un prompt que genera automáticamente Dockerfile y docker-compose.yml para crear un entorno de ejecución de aplicaciones en contenedores."
tags: ["Docker", "Contenedor", "Despliegue", "DevOps"]
lang: es
---

# 📝 Construcción de entorno Docker, listo en 3 minutos con un archivo de configuración

> **🎯 Recomendado para:** Todos
> **⏱️ Tiempo:** 5 minutos
> **🤖 Modelo recomendado:** Todos los modelos de IA

| Dificultad | Efectividad | Utilidad  |
| :--------: | :---------: | :-------: |
|  ⭐⭐☆☆☆   | ⭐⭐⭐⭐⭐  | ⭐⭐⭐⭐☆ |

_"Da error porque la versión de Node.js es diferente en el servidor de despliegue..."_

La discrepancia entre el entorno de desarrollo y el de operación es el enemigo de todo desarrollador.
Sabemos que usar Docker lo soluciona, pero escribir el `Dockerfile` y optimizar la construcción de la imagen es otra barrera.
Solo dinos el stack del proyecto. La IA te escribirá el archivo de configuración de Docker optimizado.

---

## ⚡️ Resumen en 3 líneas (TL;DR)

1. Generación de `Dockerfile` optimizado para el lenguaje/framework.
2. Creación de `docker-compose.yml` incluyendo DB, caché, etc.
3. Aplicación de optimización de tamaño de imagen (Multi-stage Build).

---

## 🚀 Solución: "Experto en Dockerización"

### 🥉 Versión Básica

Úsala cuando necesites resultados rápidos.

> **Rol:** Eres un experto en orquestación de contenedores e ingeniero DevOps.
> **Solicitud:** Un prompt que genera automáticamente Dockerfile y docker-compose.yml para crear un entorno de ejecución de aplicaciones en contenedores.

<br>

### 🥇 Versión Pro (Experto)

Úsala cuando necesites calidad detallada.

Copia y pega el siguiente **PROMPT** en ChatGPT o Claude.

> **Rol:** Eres un experto en orquestación de contenedores e ingeniero DevOps.
>
> **Situación:** Quiero desplegar mi aplicación como un contenedor Docker. Quiero unificar el entorno de desarrollo local y el entorno de despliegue.
>
> **Solicitud:**
>
> 1. Escribe un **Dockerfile** optimizado para el siguiente stack tecnológico. (Se recomienda usar Multi-stage build)
> 2. Escribe un archivo **docker-compose.yml** que incluya la base de datos, etc., que se integra con la app.
> 3. Dime también qué contenido debe incluir el archivo `.dockerignore`.
>
> **Stack tecnológico:**
> `[Escribe el lenguaje y versión, framework, DB necesaria, etc. Ej: Node.js 18, NestJS, PostgreSQL, Redis]`
>
> **Restricciones:**
>
> - Minimiza el tamaño de la imagen considerando el entorno de producción (Ej: usar Alpine Linux).
> - Configúralo para que se ejecute como un usuario normal y no con permisos de root por seguridad.

---

## 🧬 Anatomía del Prompt (Why it works?)

---

## 📊 Prueba: Antes y Después

### ❌ Antes (Stack de entrada)

"Uso Python 3.9, Django, PostgreSQL."

### ✅ Después (Resultado - Parte del Dockerfile)

```dockerfile
# 1. Build Stage
FROM python:3.9-slim as builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --user -r requirements.txt

# 2. Production Stage
FROM python:3.9-slim
WORKDIR /app
# Copiar solo los paquetes instalados en la etapa de build
COPY --from=builder /root/.local /root/.local
COPY . .

# Configuración de variables de entorno
ENV PATH=/root/.local/bin:$PATH
# Crear y cambiar a usuario non-root por seguridad
RUN useradd -m myuser
USER myuser

CMD ["gunicorn", "config.wsgi:application", "--bind", "0.0.0.0:8000"]
```

_Características: imagen slim, build multi-stage, ejecución con usuario non-root._

---

## 🎯 Conclusión

"En mi ordenador funciona..." ya es cosa del pasado.
Empaquétalo en un contenedor llamado Docker para que se ejecute igual en cualquier lugar. ¡La IA te enseñará esa técnica de empaquetado! 🍷
