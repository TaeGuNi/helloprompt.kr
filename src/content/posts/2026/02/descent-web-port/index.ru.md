---
title: "Descent, портирован в веб"
date: 2026-02-15
categories: ["Web", "GameDev", "WASM"]
tags: ["Three.js", "WebGL", "Descent", "Retro"]
cover: "./cover.png"
---

# 📝 Descent (1995) в браузере: Анализ порта и архитектуры с помощью AI

- **🎯 Целевая аудитория:** Веб-разработчики, GameDev-энтузиасты, инженеры производительности
- **⏱️ Экономия времени:** Около 5 часов → 5 минут
- **🤖 Рекомендуемые модели:** Claude 3.5 Sonnet, GPT-4o, Gemini 2.5 Flash

- ⭐ **Сложность:** ⭐⭐⭐☆☆
- ⚡️ **Эффективность:** ⭐⭐⭐⭐⭐
- 🚀 **Полезность:** ⭐⭐⭐⭐☆

> _"Хотите узнать, как заставить браузер плавно рендерить классический 3D-шутер 95-го года, не переписывая весь движок с нуля на JavaScript?"_

Классическая игра **Descent**, выпущенная в 1995 году, получила второе дыхание — она была филигранно портирована в веб с использованием **Three.js** и **WebAssembly (WASM)**. Этот порт является блестящим примером производительности и безграничных возможностей современных браузеров.

Вы можете поиграть в неё прямо сейчас: [three-descent](https://mrdoob.github.io/three-descent/).
Но вместо того чтобы просто играть, давайте используем этот проект как учебный полигон. С помощью правильного ИИ-промпта мы можем «препарировать» этот порт и научиться связывать старый C-код с современным WebGL-рендерером для собственных ретро-проектов.

---

## ⚡️ Краткое содержание (TL;DR)

1. **Descent в браузере:** Культовый 3D-шутер работает в вебе благодаря связке Three.js (графика) и WASM (логика).
2. **Идеальный референс:** Проект демонстрирует правильный подход к разделению ответственности между игровым движком и слоем отрисовки.
3. **AI как ментор:** Представленные промпты помогут вам быстро разобраться в архитектуре подобных портов и сгенерировать стартовый шаблон для своих идей.

---

## 🚀 Решение: Промпт "Архитектор WASM-портов"

### 🥉 Базовая версия (Basic)

Быстрый способ получить общее представление о технологическом стеке и принципах работы.

> **Роль:** Ты — Senior WebGL Engineer.
> **Запрос:** Объясни мне понятным языком, как связаны Three.js и WebAssembly в проекте порта игры Descent (1995) в браузер. Дай краткую выжимку архитектуры и распределения задач между JS и WASM.

<br>

### 🥇 Продвинутая версия (Pro)

Используйте этот промпт для глубокого технического анализа и генерации бойлерплейт-кода для собственного порта легаси-игры.

> **Роль (Role):** Ты — `[Lead Game Engine Developer]`, специализирующийся на портировании легаси-кода (C/C++) в веб-среду с использованием WebAssembly и WebGL/Three.js.
>
> **Контекст (Context):**
>
> - Исходные данные: Классическая игра Descent (1995) была успешно портирована для работы в браузере (пример: https://mrdoob.github.io/three-descent/).
> - Цель: Я хочу глубоко понять паттерны интеграции между старым C-кодом, скомпилированным в WASM (обработка физики, логики, ввода), и современным рендерингом на Three.js. Я планирую применить этот архитектурный подход для портирования `[название вашей старой игры, например: Doom, Quake или кастомный движок]`.
>
> **Задача (Task):**
>
> 1. Проанализируй вероятную архитектуру такого порта: как именно WASM эффективно передает данные о трансформациях объектов (матрицы, позиции, кватернионы) в цикл отрисовки Three.js без потери производительности.
> 2. Напиши пошаговый план портирования старого C-движка в веб.
> 3. Сгенерируй базовый шаблон кода (HTML + JavaScript + пример заглушки C/WASM) для связи логики игры (Game Loop) и рендера (Render Loop).
>
> **Ограничения (Constraints):**
>
> - Сгенерированный код должен использовать современные стандарты ES6+ и актуальный синтаксис Three.js.
> - Избегай лишней воды, пиши максимально техническим, инженерным языком.
> - Выведи план портирования в виде структурированного списка с чекбоксами.
>
> **Предупреждение (Warning):**
>
> - Не выдумывай несуществующие API Three.js или Emscripten. Опирайся исключительно на официальную и проверенную документацию. Если в чем-то не уверен — прямо скажи об этом.

---

## 💡 Комментарий автора (Insight)

Портирование ретро-игр в веб перестало быть магией, доступной лишь избранным хакерам. Главный секрет кроется в жестком разделении ответственности: пусть скомпилированный через Emscripten модуль WASM забирает на себя всю "тяжелую" математику, физику коллизий и логику игры, а Three.js занимается исключительно красивой отрисовкой кадров. С помощью предложенного промпта вы сможете сэкономить десятки часов на мучительном чтении документации по интеграции памяти между JavaScript и C++, сразу получив рабочий каркас для смелых экспериментов.

---

## 🙋 Часто задаваемые вопросы (FAQ)

- **Q: Нужны ли глубокие знания C++ для использования этого промпта?**
  - A: Базовое понимание концепций памяти (указатели, буферы) в C/C++ очень поможет. Однако ИИ сгенерирует подробные комментарии и связующий JS-код, что радикально снижает порог вхождения даже для фронтенд-разработчиков.

- **Q: Можно ли применить этот подход для современных 3D-игр?**
  - A: Современные гиганты вроде Unity или Unreal Engine уже "из коробки" умеют экспортировать проекты в WebGL/WASM. Представленный подход наиболее ценен именно для работы с кастомными легаси-движками или чистыми C++ проектами, которые вы хотите "оживить" в браузере.

---

## 🧬 Разбор промпта (Why it works?)

1. **Четкое позиционирование (Role):** Задав роль `Lead Engine Developer`, мы гарантируем, что нейросеть не скатится в банальное объяснение основ "что такое WebAssembly", а сразу сфокусируется на архитектурных паттернах и низкоуровневой интеграции.
2. **Конкретный референс (Context):** Упоминание реально существующего порта Descent задает ИИ правильную планку качества и дает точку опоры для понимания уровня сложности поставленной задачи.
3. **Превентивная защита (Warning):** Жесткое ограничение на выдумывание API защищает нас от типичных галлюцинаций, когда ИИ предлагает несуществующие методы для работы с памятью в Emscripten.

---

## 📊 Результат: До и После

### ❌ До (Самостоятельный поиск)

```text
(Часы блуждания по StackOverflow в попытках понять, как передавать массивы указателей из C++ в JavaScript для обновления позиций 3D-моделей в Three.js, сопровождающиеся постоянными утечками памяти и падением FPS).
```

### ✅ После (Результат работы промпта)

```javascript
// ИИ мгновенно генерирует готовый мост между WASM и Three.js
function updateGameLogic() {
  // 1. Вызываем шаг игровой логики из WASM
  wasmModule._tick();

  // 2. Получаем указатель на буфер с позициями объектов
  const positionPtr = wasmModule._getEntitiesPositions();
  const entityCount = wasmModule._getEntityCount();

  // 3. Создаем типизированный массив для быстрого чтения памяти WASM
  const positions = new Float32Array(
    wasmModule.HEAPF32.buffer,
    positionPtr,
    entityCount * 3,
  );

  // 4. Обновляем инстансы в Three.js
  for (let i = 0; i < entityCount; i++) {
    const mesh = entityMeshes[i];
    mesh.position.set(
      positions[i * 3],
      positions[i * 3 + 1],
      positions[i * 3 + 2],
    );
  }
}
```

---

## 🎯 Заключение

Возможности современных веб-браузеров действительно впечатляют, а грамотная связка WebAssembly и Three.js открывает невероятные горизонты для портирования вечной классики. Вместо того чтобы изобретать велосипед или тонуть в низкоуровневой отладке, используйте ИИ в качестве своего технического лида. Это поможет вам быстро собрать надежный архитектурный каркас и наслаждаться возрождением ретро-шедевров прямо в вебе!

Теперь ваша очередь портировать любимую игру детства. Удачи в кодинге, и пусть ваш FPS всегда будет высоким! 🎮
