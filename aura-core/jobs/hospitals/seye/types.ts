import type { HospitalProduct, HospitalProductOption } from '../../core/types.js';

export type SeyeOption = HospitalProductOption;

export interface SeyeProduct extends HospitalProduct {
    // Seye specific fields (if any in future)
}
