---
title: "Медленный SQL-запрос, секрет настройки за 3 секунды"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Кодинг/Разработка"
description: "Промпт, который анализирует сложные и неэффективные SQL-запросы для оптимизации производительности и рекомендации индексов."
tags: ["SQL", "Тюнинг БД", "Оптимизация запросов", "База данных"]
---

# 📝 Медленный SQL-запрос, секрет настройки за 3 секунды

**🎯 Рекомендуется для:** Всех
**⏱️ Требуемое время:** 5 минут
**🤖 Рекомендуемая модель:** Все модели ИИ

| Сложность | Эффективность | Полезность |
| :-------: | :-----------: | :--------: |
|  ⭐⭐☆☆☆  |  ⭐⭐⭐⭐⭐   | ⭐⭐⭐⭐☆  |

_"Даже с небольшим количеством накопленных данных запрос занимает 5 секунд..."_

Запросы, которые поначалу были быстрыми, становятся черепахами по мере увеличения данных.
Вы чувствовали себя потерянным, глядя на План выполнения (Explain Plan), потому что черное — это буквы, а белое — фон?
Если вы покажете запрос ИИ, он найдет неэффективные части и даже даст волшебные рекомендации по индексам.

---

## ⚡️ Краткое содержание в 3 строках (TL;DR)

1. Улучшение неэффективных Joins и подзапросов
2. Рекомендация необходимых индексов
3. Переписать с читаемым современным синтаксисом SQL

---

## 🚀 Решение: "Тюнер SQL-запросов"

### 🥉 Базовая версия

Используйте это, когда вам нужен просто быстрый результат.

**Роль:** Ты главный DBA (Администратор базы данных) с 20-летним опытом и эксперт по тюнингу SQL.
**Запрос:** Промпт, который анализирует сложные и неэффективные SQL-запросы для оптимизации производительности и рекомендации индексов.

<br>

### 🥇 Профессиональная версия (Эксперт)

Используйте это, когда вам нужно детальное качество.

Скопируйте содержание **PROMPT** ниже и вставьте его в ChatGPT или Claude.

**Роль:** Ты главный DBA (Администратор базы данных) с 20-летним опытом и эксперт по тюнингу SQL.

**Контекст:** SQL-запрос, используемый в моем приложении, вызывает снижение производительности по мере увеличения объема данных.

**Задача:**
Проанализируй предоставленный ниже SQL-запрос и выполни следующие задачи:

1. **Диагностика проблемы:** Укажи части, которые могут вызвать снижение производительности в текущем запросе (Full Table Scan и т.д.).
2. **Оптимизация запроса:** Перепиши в запрос, который возвращает тот же результат, но работает более эффективно.
3. **Рекомендация индекса:** Предложи, какие столбцы индексировать с помощью синтаксиса `CREATE INDEX`, чтобы максимизировать производительность запроса.

**Используемая БД:** `[MySQL / PostgreSQL / Oracle / MS-SQL]`

**Целевой запрос:**

```sql
[Вставьте медленный запрос сюда]
```

**Ограничения:**

- Набор данных результата существующего запроса никогда не должен меняться.
- Кратко объясни принцип выполнения, почему он стал быстрее.

---

## 🧬 Анатомия промпта (Почему это работает?)

---

## 📊 Доказательство: До и После

### ❌ До (Входной запрос)

```sql
SELECT * FROM users
WHERE id IN (SELECT user_id FROM orders WHERE price > 10000);
```

_Проблема: Подзапрос может выполняться для каждой строки (Зависимый подзапрос)._

### ✅ После (Результат)

**Оптимизированный запрос (Использование JOIN):**

```sql
SELECT DISTINCT u.*
FROM users u
INNER JOIN orders o ON u.id = o.user_id
WHERE o.price > 10000;
```

**Рекомендуемый индекс:**

```sql
CREATE INDEX idx_orders_price_userid ON orders(price, user_id);
```

_Объяснение: Оптимизировано путем изменения подзапроса на JOIN и создания составного индекса для соединения по `user_id` после фильтрации по `price`._

---

## 🎯 Заключение

Говорят, что "Тюнинг — это искусство", но теперь это 'наука', где вы можете получить помощь от ИИ.
Не страдайте от медленных запросов, получите бесплатную консультацию от ИИ-DBA! 🍷
