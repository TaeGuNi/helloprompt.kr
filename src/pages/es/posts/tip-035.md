---
layout: /src/layouts/Layout.astro
title: "¿Mi código puede ser hackeado? Comprobación de vulnerabilidades del Sheriff IA"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Desarrollo"
description: "Un prompt que encuentra vulnerabilidades de seguridad ocultas en mi código como Inyección SQL, XSS, etc., y propone parches de código."
tags:
  [
    "Seguridad",
    "Defensa contra hacking",
    "Vulnerabilidad",
    "Secure Coding",
    "OWASP",
  ]
lang: es
---

# 📝 ¿Mi código puede ser hackeado? Comprobación de vulnerabilidades del Sheriff IA

> **🎯 Recomendado para:** Todos
> **⏱️ Tiempo:** 5 minutos
> **🤖 Modelo recomendado:** Todos los modelos de IA

| Dificultad | Efectividad | Utilidad  |
| :--------: | :---------: | :-------: |
|  ⭐⭐☆☆☆   | ⭐⭐⭐⭐⭐  | ⭐⭐⭐⭐☆ |

_"¿Quién va a hackear mi proyecto personal?"_

Sí, lo hacen. Los bots automatizados rastrean internet sin descanso buscando servidores vulnerables.
Una inyección SQL puede robar tu base de datos de un golpe, y un ataque XSS puede secuestrar las cookies de los usuarios.
Antes de desplegar, muestra tu código al Sheriff IA y recibe una receta de 'Secure Coding'.

---

## ⚡️ Resumen en 3 líneas (TL;DR)

1. Identificación de vulnerabilidades de seguridad potenciales en el código (SQL Injection, XSS, etc.).
2. Análisis de riesgo basado en OWASP Top 10.
3. Propuesta de corrección con código seguro.

---

## 🚀 Solución: "Escáner de Vulnerabilidades de Seguridad"

### 🥉 Versión Básica

Úsala cuando necesites resultados rápidos.

> **Rol:** Eres un hacker de sombrero blanco y experto en ciberseguridad.
> **Solicitud:** Un prompt que encuentra vulnerabilidades de seguridad ocultas en mi código como Inyección SQL, XSS, etc., y propone parches de código.

<br>

### 🥇 Versión Pro (Experto)

Úsala cuando necesites calidad detallada.

Copia y pega el siguiente **PROMPT** en ChatGPT o Claude.

> **Rol:** Eres un hacker de sombrero blanco y experto en ciberseguridad.
>
> **Situación:** Quiero comprobar si hay partes peligrosas en cuanto a seguridad en el código que he escrito.
>
> **Solicitud:**
>
> 1. Analiza el código a continuación y encuentra todas las **vulnerabilidades de seguridad (Vulnerability)**.
> 2. Explica a qué tipo de ataque (ej: SQL Injection, XSS) puede estar expuesta cada vulnerabilidad.
> 3. Escribe el **código seguro (Secure Code)** que solucione las vulnerabilidades.
>
> **Código a analizar:**
>
> ```
> [Pega aquí el código sospechoso]
> ```
>
> **Restricciones:**
>
> - Revisa priorizando los criterios de vulnerabilidad OWASP Top 10.
> - Menciona brevemente el escenario de "cómo se puede atacar" en lugar de solo decir "es peligroso".

---

## 🧬 Anatomía del Prompt (Why it works?)

---

## 📊 Prueba: Antes y Después

### ❌ Antes (Código de entrada - PHP)

```php
$username = $_POST['user'];
$query = "SELECT * FROM users WHERE name = '" . $username . "'";
$db->query($query);
```

### ✅ Después (Diagnóstico y Corrección)

**🚨 Vulnerabilidad detectada:** **SQL Injection**
Si un atacante introduce un valor como `' OR '1'='1` en `$username`, la consulta se convierte en `SELECT * FROM users WHERE name = '' OR '1'='1'`, permitiendo ver toda la información de los usuarios.

**🛡️ Código corregido (Usando Prepared Statement):**

```php
$stmt = $db->prepare("SELECT * FROM users WHERE name = ?");
$stmt->bind_param("s", $_POST['user']);
$stmt->execute();
```

_Explicación: Se bloqueó la inyección en origen utilizando `prepare` y `bind_param` para tratar el valor de entrada como datos puros y no como parte de la consulta._

---

## 🎯 Conclusión

Los incidentes de seguridad no se pueden arreglar después de que ocurren.
No te conviertas en el protagonista del dicho "cerrar el establo cuando se han escapado las vacas". Corregir una línea de código puede prevenir una catástrofe. 🍷
