# 📊 데이터 분석 가이드 (Analytics Guide)

Hello Prompt는 **Google Analytics 4 (GA4)**를 사용하여 사용자 행동을 정밀하게 분석합니다.
단순한 페이지 뷰(PV)를 넘어, **"사용자가 프롬프트를 어떻게 소비하고 활용하는지"** 이해하는 것이 목표입니다.

---

## 1. 개요 (Overview)

- **Platform:** Google Analytics 4 (GA4)
- **Measurement ID:** `G-P5MFF2LHX1`
- **Implementation:** `src/layouts/Layout.astro` (Global Script)

---

## 2. 수집 이벤트 (Tracked Events)

우리는 사용자의 여정(Journey)을 5단계로 나누어 추적합니다.

### 🔍 탐색 (Discovery)

사용자가 콘텐츠를 어떻게 찾는지 분석합니다.

| 이벤트명 (Event Name) | 설명 (Description) | 주요 파라미터 (Parameters) |
| :--- | :--- | :--- |
| **`select_content`** | 검색 결과 또는 추천 글 클릭 | `content_type`: "search_result" / "related_post"<br>`search_term`: 검색어 (검색 시)<br>`item_id`: 클릭한 글 제목 |
| **`click_tag`** | 태그 클릭 | `tag_name`: 클릭한 태그명<br>`location`: "home_trending" (메인) / "article_header" (상세) |
| **`change_language`** | 언어 변경 | `event_label`: 변경된 언어 코드 (en, ko 등)<br>`from_lang`: 변경 전 언어 |

### 📖 소비 (Consumption)

사용자가 글을 얼마나 깊이 있게 읽는지 분석합니다.

| 이벤트명 (Event Name) | 설명 (Description) | 주요 파라미터 (Parameters) |
| :--- | :--- | :--- |
| **`click_toc`** | 목차(TOC) 클릭 | `section_heading`: 이동한 섹션 제목<br>`page_title`: 현재 글 제목 |
| **`view_404`** | 존재하지 않는 페이지 접근 | `page_path`: 접근 시도한 URL |

### ✍️ 활용 (Action) - **CORE**

**Hello Prompt의 핵심 가치**인 "프롬프트 활용"을 추적합니다.

| 이벤트명 (Event Name) | 설명 (Description) | 주요 파라미터 (Parameters) |
| :--- | :--- | :--- |
| **`copy_prompt`** | 프롬프트 복사 버튼 클릭 | `prompt_text`: 복사한 텍스트 (앞 100자)<br>`page_title`: 글 제목 |
| **`customize_prompt`** | **[빈칸]** 채우기 및 수정 | `input_value`: 사용자가 입력한 값<br>`original_text`: 빈칸의 원래 플레이스홀더<br>`page_title`: 글 제목 |
| **`use_ai_tool`** | AI 도구 바로가기 클릭 | `tool_name`: "ChatGPT", "Claude", "Gemini" 등 |

### ❤️ 참여 (Engagement)

사용자의 충성도 및 공유 행위를 분석합니다.

| 이벤트명 (Event Name) | 설명 (Description) | 주요 파라미터 (Parameters) |
| :--- | :--- | :--- |
| **`bookmark`** | 북마크 추가/해제 | `action`: "add" / "remove"<br>`page_title`: 글 제목 |
| **`share`** | 공유 버튼 클릭 | `method`: "link_copy" / "twitter"<br>`item_id`: 공유된 글 제목 |

---

## 3. 데이터 활용 시나리오 (Use Cases)

수집된 데이터는 다음과 같은 질문에 답하는 데 사용됩니다.

1.  **콘텐츠 기획:** "사람들이 검색창에 가장 많이 입력하는 키워드는 무엇인가?" (`select_content` + `search_term`)
2.  **UX 개선:** "메인 화면의 인기 태그(`home_trending`)가 효과적인가, 아니면 검색(`search_result`)을 더 선호하는가?"
3.  **프롬프트 최적화:** "사용자들이 프롬프트의 **[빈칸]**에 주로 어떤 내용을 채워 넣는가?" (`customize_prompt`)
4.  **서비스 확장:** "사용자들이 ChatGPT와 Claude 중 어느 도구로 더 많이 이동하는가?" (`use_ai_tool`)

---

## 4. 구현 상세 (Implementation Details)

- **`src/layouts/Layout.astro`:** GA4 스크립트 초기화 및 전역 이벤트(공유, 북마크, 프롬프트 복사/수정) 리스너가 포함되어 있습니다.
- **`src/components/Search.astro`:** 검색 결과 클릭 이벤트를 처리합니다.
- **`src/components/RelatedPosts.astro`:** 관련 글 클릭 이벤트를 처리합니다.
- **`src/pages/404.astro`:** 404 에러 발생 시 이벤트를 전송합니다.

> **Note:** 모든 이벤트는 `gtag()` 함수가 로드된 상태에서만 안전하게 발송되도록 방어 코드가 작성되어 있습니다.
