---
layout: /src/layouts/Layout.astro
title: "Бросьте JSON — получите TypeScript типы! (с Zod)"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Разработка"
description: "Промпт, который автоматически преобразует сложные данные JSON в интерфейсы TypeScript или схемы Zod."
tags: ["TypeScript", "Zod", "JSON", "ОпределениеТипов", "Продуктивность"]
lang: ru
---

# 📝 Бросьте JSON — получите TypeScript типы! (с Zod)

> **🎯 Рекомендуется для:** Всех
> **⏱️ Время:** 5 минут
> **🤖 Рекомендуемая модель:** Все модели ИИ

| Сложность | Эффективность | Полезность |
| :-------: | :-----------: | :--------: |
|  ⭐⭐☆☆☆  |  ⭐⭐⭐⭐⭐   | ⭐⭐⭐⭐☆  |

_"Ответ API с бэкенда такой сложный, когда я все это опишу в интерфейсах?"_

Вручную пишете `interface User { ... }`?
Вложенные объекты, массивы, опциональные значения... Легко ошибиться.
Просто скопируйте и вставьте JSON ответа API. ИИ создаст идеальные определения типов TypeScript и даже схемы Zod для проверки во время выполнения.

---

## ⚡️ Краткое содержание (TL;DR)

1. Преобразование данных JSON в TypeScript Interface/Type.
2. Генерация схем Zod/Yup для валидации во время выполнения.
3. Идеальная обработка вложенных структур (Nested Object).

---

## 🚀 Решение: "Мастер типов"

### 🥉 Базовая версия (Basic Version)

Используйте, когда вам нужен быстрый результат.

> **Роль:** Ты ведущий разработчик, владеющий экосистемой TypeScript.
> **Запрос:** Автоматически преобразуй сложные данные JSON в интерфейсы TypeScript или схемы Zod.

<br>

### 🥇 Про-версия (Pro Version)

Используйте, когда вам нужно детальное качество.

Скопируйте **PROMPT** ниже и вставьте его в ChatGPT или Claude.

> **Роль:** Ты ведущий разработчик, владеющий экосистемой TypeScript.
>
> **Ситуация:** Я получил данные JSON от внешнего API и мне нужно определить типы для безопасного использования в моем проекте.
>
> **Запрос:**
>
> 1. На основе данных JSON ниже напиши **TypeScript Interface**. (Имена в PascalCase)
> 2. Также напиши **схему Zod (Schema)** для валидации во время выполнения.
> 3. Включи код для вывода типов из схемы Zod (`z.infer`).
>
> **Данные JSON:**
>
> ```text
> [Вставьте сюда данные JSON]
> ```
>
> **Ограничения:**
>
> - Считай все поля обязательными (required), но если встречается `null`, обрабатывай как опциональные (`?`).
> - Объекты внутри массивов выноси в отдельные интерфейсы.
> - Добавь JSDoc комментарии, объясняющие, что это за поле (предположительно).

---

## 🧬 Разбор промпта (Почему это работает?)

---

## 📊 Доказательство: До и После

### ❌ До (Ввод JSON)

```text
{
  "id": 1,
  "name": "Jay",
  "contact": {
    "email": "jay@test.com",
    "phone": null
  },
  "tags": ["developer", "admin"]
}
```

### ✅ После (Результат)

```typescript
import { z } from "zod";

// 1. Zod Schema Definition
export const ContactSchema = z.object({
  email: z.string().email(),
  phone: z.string().nullable().optional(), // обработка null
});

export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  contact: ContactSchema,
  tags: z.array(z.string()),
});

// 2. TypeScript Type Inference
/** Интерфейс информации о пользователе */
export type User = z.infer<typeof UserSchema>;
export type Contact = z.infer<typeof ContactSchema>;
```

---

## 🎯 Заключение

Определение типов — это страховка, но процесс ее оформления не должен быть мучительным.
Создайте надежную систему типов одним копипастом! 🍷
