---
layout: /src/layouts/Layout.astro
title: "Рефакторинг грязного кода в чистый за 1 секунду"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "Кодинг/Разработка"
description: "Промпт, который превращает сложный спагетти-код в легко читаемый и поддерживаемый чистый код."
tags: ["Рефакторинг", "Чистый код", "Поддержка", "Продуктивность разработчика"]
---

# 📝 Рефакторинг грязного кода в чистый за 1 секунду

**🎯 Рекомендуется для:** Всех
**⏱️ Требуемое время:** 5 минут
**🤖 Рекомендуемая модель:** Все модели ИИ

| Сложность | Эффективность | Полезность |
| :-------: | :-----------: | :--------: |
|  ⭐⭐☆☆☆  |  ⭐⭐⭐⭐⭐   | ⭐⭐⭐⭐☆  |

_"Я даже не могу узнать код, который сам написал... Как распутать это спагетти?"_

У всех нас был опыт написания беспорядочных имен переменных, потому что мы спешили, или когда одна функция превышала 100 строк.
Что если бы был секретарь, который рефакторил бы код, который вам стыдно показывать коллегам, в **стандарты 'Чистого кода'**?
С помощью всего одного этого промпта вы можете одновременно улучшить читаемость, производительность и поддерживаемость.

---

## ⚡️ Краткое содержание в 3 строках (TL;DR)

1. Интуитивно измените имена переменных/функций
2. Удалите дублирующийся код и разделите функции
3. Примените новейший синтаксис и оптимизируйте производительность

---

## 🚀 Решение: "Рефактор чистого кода"

### 🥉 Базовая версия

Используйте это, когда вам нужен просто быстрый результат.

**Роль:** Ты старший инженер-программист с 20-летним опытом и эксперт по 'Чистому коду'.
**Запрос:** Преврати сложный спагетти-код в легко читаемый и поддерживаемый чистый код.

<br>

### 🥇 Профессиональная версия (Эксперт)

Используйте это, когда вам нужно детальное качество.

Скопируйте содержание **PROMPT** ниже и вставьте его в ChatGPT или Claude.

**Роль:** Ты старший инженер-программист с 20-летним опытом и эксперт по 'Чистому коду'.

**Контекст:** Код, который я написал, работает, но он плохо читается и его трудно поддерживать. Имена переменных неясны, и много дублирующейся логики.

**Задача:**
Отрефактори код, предоставленный ниже, в соответствии со следующими критериями:

1. **Именование:** Измени имена переменных и функций так, чтобы их назначение было четко понятно.
2. **Разделение функций:** Разбей функции на более мелкие части, чтобы одна функция делала только одну вещь (SRP).
3. **Удаление дубликатов:** Извлеки повторяющуюся логику в отдельные функции или утилиты.
4. **Комментарии:** Добавляй комментарии только к частям, где требуется объяснение 'Почему' код был написан таким образом.
5. **Современный синтаксис:** Примени новейший современный синтаксис языка.

**Код:**

```
[Вставьте код для рефакторинга сюда]
```

**Ограничения:**

- Никогда не меняй бизнес-логику (поведение) существующего кода.
- Обобщи основные изменения до и после рефакторинга в списке.
- Показывай вывод, разделенный на блок кода и блок объяснения.

---

## 🧬 Анатомия промпта (Почему это работает?)

---

## 📊 Доказательство: До и После

### ❌ До (Ввод)

```javascript
function calc(d) {
  let t = 0;
  for (let i = 0; i < d.length; i++) {
    if (d[i].price > 100) {
      t += d[i].price * 0.9;
    } else {
      t += d[i].price;
    }
  }
  return t;
}
```

### ✅ После (Результат)

```javascript
/**
 * Вычисляет общую сумму корзины.
 * Применяет скидку 10% для товаров дороже 100 вон.
 */
function calculateTotalPrice(items) {
  const DISCOUNT_THRESHOLD = 100;
  const DISCOUNT_RATE = 0.9;

  return items.reduce((total, item) => {
    const finalPrice =
      item.price > DISCOUNT_THRESHOLD ? item.price * DISCOUNT_RATE : item.price;
    return total + finalPrice;
  }, 0);
}
```

_Изменения: `calc` -> `calculateTotalPrice`, `d` -> `items`, магические числа в константы, использовано `reduce`._

---

## 🎯 Заключение

Больше не откладывайте, говоря "Я исправлю это позже".
Превратите вонючий код в ароматный код за 3 секунды с помощью этого промпта и идите домой! 🍷
