---
layout: /src/layouts/Layout.astro
title: "복잡한 코드는 이제 그만! AI 복잡도 분석기 (Code Complexity Analyzer)"
author: HelloBot
date: 2026-02-12
updatedDate: 2026-02-12
category: Development
description: " \"スパゲティコードを分析し、明確なリファクタリングのポイントを提案するAIプロンプトです。複雑なロジックを解きほぐし、シンプルで保守性の高いコードへと導きます。\""
tags: [AI, Refactoring, Clean Code, Development]
---

# 🍝 複雑なコードにさようなら！AIコード複雑度アナライザー (Code Complexity Analyzer)

- **🎯 おすすめの対象:** ジュニアエンジニア、レガシーコードを引き継いだ保守担当者、コードレビュアー
- **⏱️ 所要時間:** 1時間 → 3分に短縮
- **🤖 おすすめのモデル:** GPT-4o, Claude 3.5 Sonnet (コードの推論能力に優れたモデルを推奨)

- ⭐ **難易度:** ⭐⭐⭐☆☆
- ⚡️ **効果:** ⭐⭐⭐⭐⭐
- 🚀 **活用度:** ⭐⭐⭐⭐⭐

> _「if文の中にfor文、その中にまたif文…動いてはいるけれど、少しでも触ると爆発しそうな時限爆弾のようなコードに絶望したことはありませんか？」_

開発を進めていると、必然的に複雑度の高い「スパゲティコード」に直面します。機能の追加やバグ修正が繰り返されるうちにコードは肥大化し、循環的複雑度（Cyclomatic Complexity）は跳ね上がります。このようなコードは読みにくいだけでなく、修正時の副作用（サイドエフェクト）を予測しづらいため、保守地獄の主な原因となります。

しかし、どこからどう手をつければいいのか途方に暮れていませんか？そんな時こそ、AIにコード複雑度の分析を任せてみましょう。客観的な指標で問題点を診断し、デザインパターンやクリーンコードの原則に基づいた具体的なリファクタリング戦略を提示してくれます。

---

## ⚡️ 3行まとめ (TL;DR)

1. 読みにくいネストされた条件文や巨大な関数をAIが構造的に分析します。
2. 早期リターン（Early Return）や関数の抽出（Extract Method）など、実務的なリファクタリング手法の提案を受けられます。
3. ロジックの変更なしに、可読性と保守性が最大化されたクリーンなコードを手に入れることができます。

---

## 🚀 解決策：「コード複雑度分析＆リファクタリングプロンプト」

### 🥉 Basic Version (基本型)

素早くコードの問題点だけを把握したい場合に使用してください。

> **役割:** あなたは`[シニアバックエンドエンジニア]`です。
> **タスク:** 以下のコードの複雑度を分析し、どのようにリファクタリングすべきか3つのポイントで指摘してください。
>
>
> [ここにコードを貼り付け]
>
### 🥇 Pro Version (専門家型)

正確な指標の分析と具体的なデザインパターンの適用が必要な場合に使用してください。

> **役割 (Role):** あなたはクリーンアーキテクチャに精通した`[実務経験10年のシニアソフトウェアエンジニア]`です。
>
> **状況 (Context):**
>
> - 背景: 現在、`[言語/フレームワーク、例: TypeScript/NestJS]`で書かれたレガシーコードをリファクタリングしています。
> - 目標: コードの循環的複雑度を下げ、可読性と保守性を最大化すること。
>
> **タスク (Task):**
>
> 1. **複雑度の診断:** 提供されたコードの循環的複雑度（Cyclomatic Complexity）と認知的複雑度（Cognitive Complexity）を評価し、主な原因（深いネスト、単一責任の原則違反など）を分析してください。
> 2. **リファクタリング戦略:** ガード節（Guard Clauses）、関数の抽出（Extract Method）、ポリモーフィズム（Polymorphism）の活用など、具体的なリファクタリング手法を提案してください。
> 3. **改善されたコード:** 上記の戦略を適用し、既存のビジネスロジックは100%同一に保ちながら、構造が改善されたコードを作成してください。
>
> **コード入力 (Code):**
>
>
> [ここにリファクタリングするコードを貼り付け]
>
>
> **制約事項 (Constraints):**
>
> - 改善されたコードには、各変更点に対する短いコメント（注釈）を追加してください。
> - 説明はジュニアエンジニアでも理解できるように、丁寧かつ段階的に行ってください。
>
> **注意事項 (Warning):**
>
> - 元のコードのビジネスロジック（戻り値、副作用など）は絶対に変更しないでください。

---

## 💡 筆者のコメント (Insight)

実務で他人が書いた（あるいは過去の自分が書いた）レガシーコードをレビューしたり修正したりする必要がある際に、私が真っ先に使うプロンプトです。AIが単にコードを短くしてくれるだけでなく、「この条件文はネスト（Depth）が4段階に達しているため、Guard Clauses（ガード節）を使って外に出す方が良いです」といった論理的な根拠を提供してくれます。

特に、コードレビューを控えて自分のコードの複雑度を自己点検する際に非常に役立ちます。PR（Pull Request）を作成する前にこのプロンプトで一度フィルタリングを通すだけで、チームメンバーから「コードがとても綺麗ですね」というフィードバックをもらえる確率が飛躍的に上がります。

---

## 🙋 よくある質問 (FAQ)

- **Q: ファイル全体を一度に入力して分析してもいいですか？**
  - A: LLMのコンテキストウィンドウの制限や集中力の低下が生じるため、推奨しません。数百行を超えるファイルよりも、50〜150行程度の「特定の関数」や「単一クラス」単位に分割してプロンプトに入力する方が、はるかに鋭く正確なリファクタリング結果を得られます。

- **Q: AIがリファクタリングしたコードをそのまま本番環境（プロダクション）に適用しても安全ですか？**
  - A: 絶対にそのままコピー＆ペーストしないでください！AIは時折、ビジネスロジックの微妙なエッジケースを見落としたり、ロジックを変形（ハルシネーション）させたりする危険性があります。**必ず既存の単体テスト（Unit Test）を通過するか確認**してから反映する必要があります。

- **Q: 特定の言語やフレームワークに合わせて改善してくれますか？**
  - A: はい。Pro Versionにある`[言語/フレームワーク]`の変数に「Java/Spring Boot」や「Python/FastAPI」などを明記すれば、そのエコシステムで推奨される命名規則（ネーミングコンベンション）やデザインパターンを反映してコードを改善してくれます。

---

## 🧬 プロンプトの解剖学 (Why it works?)

1. **指標中心の診断要求:** 単に「綺麗に書いて」と頼むのではなく、「循環的複雑度」という具体的なソフトウェア工学の指標を要求することで、AIが構造的な欠陥に集中するように誘導しています。
2. **動作維持の制約 (Constraints):** リファクタリングの第一原則である「外部的な振る舞いを保つ（変更しない）」ことを明示し、AIがロジック自体を勝手に書き換えてしまうリスクを最小限に抑えました。

---

## 📊 検証: Before & After

### ❌ Before (入力: ネストの深いスパゲティコード)

```javascript
function processOrder(order) {
  if (order != null) {
    if (order.status === "PENDING") {
      if (order.items && order.items.length > 0) {
        let total = 0;
        for (let i = 0; i < order.items.length; i++) {
          if (order.items[i].price > 0) {
            total += order.items[i].price * order.items[i].quantity;
          }
        }
        if (total > 100) {
          order.discount = total * 0.1;
        } else {
          order.discount = 0;
        }
        return total - order.discount;
      } else {
        return 0;
      }
    } else {
      throw new Error("Order is not pending");
    }
  } else {
    throw new Error("Invalid order");
  }
}
```

### ✅ After (出力: ガード節と配列メソッドを活用したクリーンなコード)

```javascript
function processOrder(order) {
  // 1. Guard Clauses: 例外状況の早期リターン（ネストの削減）
  if (!order) throw new Error("Invalid order");
  if (order.status !== "PENDING") throw new Error("Order is not pending");
  if (!order.items || order.items.length === 0) return 0;

  // 2. 高階関数の活用: ループ処理の単純化
  const total = order.items
    .filter((item) => item.price > 0)
    .reduce((sum, item) => sum + item.price * item.quantity, 0);

  // 3. ビジネスロジックの分離と単純化
  const discount = total > 100 ? total * 0.1 : 0;
  order.discount = discount;

  return total - discount;
}
```

---

## 🎯 結論

複雑なコードを前に、もう頭を抱える必要はありません。AIの分析を通じて問題の核心を把握し、提案されたリファクタリング戦略を一つずつ適用していけば、保守しやすくエレガントなコードを書くことができます。

さあ、テストコードを回して、スッキリした気分で退社しましょう！ 🍷
