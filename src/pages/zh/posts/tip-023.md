---
layout: ../../layouts/Layout.astro
title: "原因不明的Bug，交给AI侦探吧"
author: "Zzabbis"
date: "2026-02-04"
updatedDate: "2026-02-04"
category: "编程/开发"
description: "光看日志完全搞不懂的Bug，只要有堆栈跟踪和代码片段，AI就能找出原因。"
tags: ["调试", "Bug修复", "故障排除", "错误解决"]
---

# 📝 原因不明的Bug，交给AI侦探吧

> **🎯 推荐对象:** 所有人
> **⏱️ 所需时间:** 5分钟
> **🤖 推荐模型:** 所有AI模型

|  难度   |   有效性   |  实用性   |
| :-----: | :--------: | :-------: |
| ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |

_"明明昨天还好好的... 到底为什么不行了？"_

最可怕的Bug是连错误信息都没有就悄悄误动作的Bug，或者是看起来太明显以至于完全找不到原因的Bug。
问同事吧，解释起来太复杂，一个人在那哼哼唧唧吗？
给AI扔个日志和代码，说不定它能从未想过的地方找出原因。

---

## ⚡️ 3行摘要 (TL;DR)

1. 错误日志分析及原因推论
2. 提议修正的代码
3. 提供防止复发的Tip

---

---

## 🚀 解决方案: "福尔摩斯调试器"

### 🥉 Basic Version (基础版)

当只需要快速获得结果时使用。

> **角色:** 你是调试天才兼看穿系统架构的'福尔摩斯'级开发者。
> **请求:** 光看日志完全搞不懂的Bug，只要有堆栈跟踪和代码片段，AI就能找出原因。

<br>

### 🥇 Pro Version (专家版)

当需要细节质量时使用。

请复制下方 **PROMPT** 内容并使用。

> **角色 (Role):** 你是调试天才兼看穿系统架构的'福尔摩斯'级开发者。
>
> **情况 (Context):** 我的程序发生了意想不到的错误。找不到原因，处境困难。
>
> **请求 (Task):**
>
> 1. 分析提供的 **错误日志** 和 **相关代码**，提出Bug的3个根本原因(Root Cause)假设。
> 2. 编写能解决最有力原因的 **修正代码**。
> 3. 让初学者也能理解地解释为什么会发生这种问题。
>
> **错误日志 (Error Log):**
>
> ```
> [在此粘贴错误信息或堆栈跟踪]
> ```
>
> **相关代码 (Code Snippet):**
>
> ```
> [在此粘贴疑似有问题的代码]
> ```
>
> **约束事项 (Constraints):**
>
> - 不要只说"修好了"，要逻辑性地解释"为什么坏了"。
> - 如果有保安上的敏感信息(密码, 密钥等)，请打码处理后再提及。

---

---

---

## 🧬 提示词解剖 (Why it works?)

---

## 📊 证明: Before & After

### ❌ Before (情况)

**错误:** `TypeError: Cannot read properties of undefined (reading 'map')`
**代码:**

```javascript
function renderList(data) {
  return data.users.map((user) => `<li>${user.name}</li>`);
}
```

### ✅ After (结果)

**分析:** `data.users` 不存在(undefined)，或者在非数组状态下调用了 `map` 函数。API响应可能尚未到达或格式已更改。

**修正代码:**

```javascript
function renderList(data) {
  // 使用可选链和默认值
  const users = data?.users || [];

  if (users.length === 0) return "<p>没有用户。</p>";
  return users.map((user) => `<li>${user.name}</li>`);
}
```

---

---

---

## 🎯 结论

别为了抓Bug加班了。
委托AI侦探调查，大家只要干净利落地应用解决后的代码就行。犯人就在这里面！ 🍷
